name: Patch operation hardening

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: patch-operation-hardening
  cancel-in-progress: false

jobs:
  patch:
    if: github.head_ref == 'agent/status-bug-hardening' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/status-bug-hardening
          fetch-depth: 0

      - name: Apply operation patches
        run: python scripts/patch-operation-hardening.py

      - name: Commit patched files
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No patch changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/inspection-app.tsx app/api/cases/import/route.ts
          git commit -m "修正附件失敗與歷史紀錄操作風險"
          git push origin HEAD:agent/status-bug-hardening
